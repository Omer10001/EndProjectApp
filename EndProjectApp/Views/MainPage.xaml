<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             NavigationPage.HasNavigationBar="False"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EndProjectApp.MainPage">

    <StackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Text="Welcome to this app" FontSize="10" Grid.Column="0" Grid.Row="0" />
        <Button Text="Login" Command="{Binding GoToLoginCommand}" HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="1" />
        <Button Text="SignUp" Command="{Binding GoToSignupCommand}" HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="2" />
        </Grid>
    </StackLayout>

</ContentPage>
