<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EndProjectApp.Views.StartPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Label Text="Welcome to this app" FontSize="30" Grid.Column="0" Grid.Row="0" />
                <Button Text="Login" Command="{Binding GoToLoginCommand}" HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="1" CornerRadius="30"  />
                <Button Text="SignUp" Command="{Binding GoToSignupCommand}" HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="2" CornerRadius="30" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>